import { ChannelData, WeeklyReport } from '../types';

// Empty default channels list - user will ingest and track their own authentic YouTube channels
export const DEFAULT_CHANNELS: ChannelData[] = [];

export const DEFAULT_WEEKLY_REPORT: WeeklyReport | null = null;
